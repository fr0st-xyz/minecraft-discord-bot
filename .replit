# the original project used replit, but i dont keeping the replit file here anyway js in case

modules = ["nodejs-20:v8-20230920-bd784b9"]
hidden = [".config", "package-lock.json", ".gitignore", '.git/*']
run = "npm install && npm start"
onBoot = "npm install"
language = "nodejs"
entrypoint= "src/index.js"